#serve
#sudo python -m SimpleHTTPServer 80
#PORT=80 sudo npm start
#. ~/.bash_profile && ionic serve
#. ~/.bash_profile && nvm i 5 && nvm use 5 && ./node_modules/http-server/bin/http-server ./www --cors -p 8080
. ~/.bash_profile && nvm i 5 && nvm use 5 && node_modules/nodemon/bin/nodemon.js server.js
